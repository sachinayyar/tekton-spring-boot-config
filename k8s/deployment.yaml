apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: app-tekton-spring-boot
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: app-tekton-spring-boot
    app.kubernetes.io/name: tekton-spring-boot
    app.kubernetes.io/part-of: tekton-spring-boot
    app.openshift.io/runtime: java
  name: argocd-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tekton-spring-boot
  template:
    metadata:
      labels:
        app: tekton-spring-boot
    spec:
      containers:
      - name: tekton-spring-boot
        imagePullPolicy: Always
        image: quay.io/sachinayyar/tekton-argocd:latest
        livenessProbe:
          failureThreshold: 1
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        ports:
        - containerPort: 8080
          protocol: TCP
        - containerPort: 8443
          protocol: TCP
        - containerPort: 8778
          protocol: TCP
        readinessProbe:
          failureThreshold: 1
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
